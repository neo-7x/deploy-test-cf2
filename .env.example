# Deploy Test — environment variables.
# Copy to .env and fill in.
#
# NOTE: On the Cloudflare Workers Deploy Button, every env var is declared
# in wrangler.toml's [vars] section (not here) so the deploy form can render
# them as plain-text inputs. This file is only used by local dev, Docker,
# and Vercel deploys.

# =============================================================================
# REQUIRED
# =============================================================================

# Postgres connection string. Format: postgresql://user:pass@host:port/db
# Works with any Postgres; no extensions required for this PoC.
#
# On Cloudflare Workers: this is used at BUILD TIME to run migrations, and must
# point at the same Postgres that the POSTGRES Hyperdrive binding is wired to.
# At runtime the Worker reads through Hyperdrive, not DATABASE_URL.
DATABASE_URL=

# Session / cookie encryption secret. Required. Generate with:
#   openssl rand -hex 32
# or use https://numbergenerator.org/random-32-digit-hex-codes-generator
BETTER_AUTH_SECRET=

# Comma-separated emails. The first sign-up with a matching email is
# auto-promoted to admin. Required — leave blank and no user will ever
# be auto-admin. Example:
#   SYSTEM_ADMIN_EMAILS=you@example.com,ops@example.com
SYSTEM_ADMIN_EMAILS=

# =============================================================================
# OPTIONAL — Sign-in methods and transactional email
# =============================================================================
# Leave any you don't need unset.
#
# If no OAuth is configured, email+password login is auto-enabled so the
# install is never locked out.
#
#   GOOGLE_CLIENT_ID=
#   GOOGLE_CLIENT_SECRET=
#   GITHUB_CLIENT_ID=
#   GITHUB_CLIENT_SECRET=
#
# Force-toggle email login. Values: true | false
# Default: on iff no OAuth is configured.
#   AUTH_EMAIL_ENABLED=
#
# When RESEND_API_KEY is set, email verification auto-activates on signup.
# Without a verified custom domain, Resend only delivers to the account
# owner's email (onboarding@resend.dev sandbox).
#   RESEND_API_KEY=
#   RESEND_FROM=
#
# Force-toggle email verification. Values: true | false
# Default: on iff email login is enabled and RESEND_API_KEY is set.
#   AUTH_EMAIL_VERIFICATION=
